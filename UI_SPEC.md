# UI 与交互设计

## 1. 信息架构

左侧导航：

1. Dashboard
2. Projects
3. Agents
4. Models
5. Router
6. Logs
7. Settings

## 2. Dashboard

展示：

- 最近项目
- 当前默认 Agent Team
- 模型连接状态
- 今日 Token / 成本
- 最近任务
- 快捷入口：新建项目、打开文件夹、配置模型、创建 Agent

## 3. Models 页面

### 3.1 Provider 列表

每个 Provider 卡片显示：

- 名称
- Provider 类型
- 模型名
- 状态：可用 / 失败 / 未测试
- 能力标签：Coding、Reasoning、Long Context、Low Cost 等
- 当前绑定的 Agent 数
- 今日调用次数
- 今日成本

### 3.2 新增 Provider 弹窗

基础字段：

- Provider 类型
- 配置名称
- Base URL
- API Key
- 模型名
- 测试连接按钮

高级字段：

- 上下文长度
- 单价
- 能力评分
- 自定义 Header
- 超时时间
- 并发限制

### 3.3 能力评分 UI

使用 0-5 星级或滑块：

- 推理
- 编码
- 长上下文
- 速度
- 低成本
- 工具调用
- 多模态
- 中文
- 本地部署

## 4. Agents 页面

### 4.1 Agent 卡片

显示：

- Agent 名称
- 角色
- 主模型
- 备用模型
- 权限等级
- 最近成功率
- 今日成本

### 4.2 Agent 编辑页

字段：

- Agent 名称
- 角色
- 职责说明
- System Prompt
- 主模型下拉框
- 备用模型排序
- 权限开关
- 预算上限
- 上下文策略

### 4.3 模型选择体验

模型下拉框需要显示：

```text
DeepSeek Pro · Coding 5 / Reasoning 5 / Cost 4
Minimax M2.7 · Agentic 5 / Coding 5 / Integration 5
Claude · Coding 5 / Long Context 5 / Review 5
Gemini · Long Context 5 / Multimodal 5
```

用户不仅看到名字，还看到为什么适合。

## 5. Router 页面

展示任务类型与默认模型路由。

例如：

| 任务类型 | 主模型 | 备用模型 | 策略 |
|---|---|---|---|
| 架构设计 | Claude | DeepSeek Pro | 质量优先 |
| 后端代码 | DeepSeek Pro | Qwen | 平衡 |
| 文档 | DeepSeek Flash | Qwen | 省钱 |
| 长上下文阅读 | Gemini | Kimi | 长上下文优先 |

用户可以修改。

## 6. Project Workspace

布局：

```text
顶部：项目名 / 当前工作模式 / 运行按钮 / 成本
左侧：文件树
中间：代码编辑器 / Diff Viewer
右侧：Agent 任务面板
底部：Terminal / Logs
```

## 7. 任务面板

每个任务显示：

- 标题
- 状态
- Agent
- 使用模型
- 推荐理由
- 预计成本
- 实际成本
- 修改文件
- 验收标准
- 操作：暂停、重试、换模型、查看日志

## 8. 执行前确认页

在任务开始前展示：

1. 本次任务清单。
2. 每个任务使用哪个 Agent。
3. 每个 Agent 使用哪个模型。
4. 为什么这样分配。
5. 预计修改文件。
6. 预计成本。
7. 风险提示。

用户可以：

- 一键开始
- 修改 Agent
- 修改模型
- 删除任务
- 调整优先级

## 9. Diff Viewer

支持：

- 单文件 Diff
- 多文件 Diff
- 按任务分组
- 按 Agent 分组
- 接受全部
- 拒绝全部
- 单文件接受
- 单 hunk 接受

## 10. Logs 页面

展示：

- 模型调用日志
- Token 使用
- 成本统计
- 错误信息
- Router 决策记录
- Agent 执行记录

日志中必须脱敏 API Key。

## 11. 用户体验原则

1. 普通用户只看到简单模式。
2. 高级用户可以展开所有模型细节。
3. 每次模型选择都给出理由。
4. 关键危险操作必须确认。
5. 成本必须透明。
6. 失败要给出下一步建议。

---

## 8. 场景驱动 Agent 组织生成器 UI

### 8.1 场景输入页

页面目标：让用户用自然语言描述想做什么。

字段：

- 产品场景输入框。
- 目标平台选择：Web、Windows、macOS、Android、iOS、Browser Extension、Backend、Script。
- 交付级别选择：Demo、MVP、Production。
- 偏好选择：速度优先、成本优先、质量优先、隐私优先。
- 已有项目路径，可选。
- 生成 Agent 团队按钮。

### 8.2 Agent 团队方案页

展示 AI 生成的 Agent Team Plan。

每个 Agent 用卡片展示：

- Agent 名称。
- 是否必需。
- 职责摘要。
- 推荐模型。
- 推荐原因。
- 预计成本。
- 权限范围。
- Prompt 预览。

页面操作：

- 接受推荐方案。
- 添加 Agent。
- 删除 Agent。
- 合并 Agent。
- 拆分 Agent。
- 更换模型。
- 编辑 Prompt。
- 查看风险变化。

### 8.3 Prompt 编辑弹窗

功能：

- 左侧展示 AI 生成 Prompt。
- 右侧用户可编辑 Prompt。
- 支持一键优化。
- 支持恢复默认。
- 支持保存为模板。
- 支持 Prompt Diff。

### 8.4 调整影响提示

用户每次调整 Agent 团队后，顶部显示影响摘要：

```text
当前方案：7 个 Agent，成本等级中等，质量等级较高。
你删除了 Test Agent，系统检测到测试覆盖风险升高。
建议至少保留 Debug Agent，或者让 Backend Agent 兼任基础测试。
```

---

## 8. Project Command Center

Project Command Center 是主界面，优先级高于传统任务看板。

### 8.1 页面定位

用户启动项目后，不应该进入“一个个点击 Agent”的页面，而应该进入项目指挥中心。

用户看到的是：

1. 主控智能体汇报。
2. 当前阶段。
3. 总进度。
4. 当前活跃 Agent。
5. 预算消耗。
6. 风险提示。
7. 待决策事项。
8. 最终交付物。

### 8.2 页面布局

```text
顶部：项目名称 / 当前阶段 / 总进度 / 自主模式 / 暂停按钮

左侧：阶段时间线
  - 场景理解
  - 执行计划
  - 架构设计
  - 代码生成
  - 集成测试
  - Debug 修复
  - 文档交付

中间：主控智能体汇报流
  - 阶段汇报
  - 风险汇报
  - 成本汇报
  - 决策请求
  - 最终交付报告

右侧：运行状态
  - 当前活跃 Agent
  - 当前使用模型
  - 任务数
  - 成本
  - 风险
  - 待审批

底部：终端 / Diff / 子 Agent 日志，可折叠
```

### 8.3 用户主要操作

用户主要只需要：

- 启动项目
- 暂停 / 继续
- 批准关键决策
- 调整自主模式
- 查看 Diff
- 验收交付

### 8.4 不作为主路径的操作

以下能力可以存在，但不应作为默认主路径：

- 手动点击某个 Agent 执行
- 手动给每个 Agent 分任务
- 手动复制日志给 Debug Agent
- 手动通知 Test Agent 运行测试
- 手动让 Doc Agent 写 README

这些应由 Orchestrator Agent 负责。

---

## 11. Project Command Center：公司式协作界面

### 11.1 页面定位

Project Command Center 是用户查看项目运行状态的主界面。

它不是传统 IDE 的任务列表，而是一个“AI 公司作战室”。

### 11.2 页面布局

```text
顶部：项目状态栏
  ├─ 当前阶段
  ├─ 运行状态
  ├─ 当前成本
  ├─ 暂停项目
  ├─ 继续执行
  └─ 回滚检查点

左侧：Agent 团队
  ├─ 当前 Agent 列表
  ├─ 每个 Agent 使用的模型
  ├─ 当前工作状态
  └─ 调整团队按钮

中间：主控汇报
  ├─ 阶段汇报
  ├─ 风险汇报
  ├─ 成本汇报
  ├─ 决策请求
  └─ 最终交付汇报

右侧：公司内部讨论
  ├─ 需求澄清会议
  ├─ 架构评审会议
  ├─ 开发同步线程
  ├─ Bug 复盘会议
  └─ 冲突仲裁记录

底部：工程执行区
  ├─ 文件 Diff
  ├─ Terminal
  ├─ 测试结果
  └─ Git 变更
```

### 11.3 暂停后的调整面板

用户点击 `暂停项目` 后，展示调整面板：

1. Agent 团队编辑。
2. 模型绑定编辑。
3. Prompt 编辑。
4. 任务范围编辑。
5. 预算编辑。
6. 权限编辑。
7. 影响评估。
8. 继续执行确认。

### 11.4 内部讨论展示规则

默认折叠，只展示摘要。

用户可展开：

1. 查看 Agent 对话摘要。
2. 查看完整线程。
3. 查看原始模型消息。
4. 查看关联文件。
5. 查看对应决策。

### 11.5 重要按钮

| 按钮 | 作用 |
|---|---|
| 暂停项目 | 停止继续派发任务，创建 Checkpoint |
| 继续执行 | 用户确认影响评估后恢复项目 |
| 调整团队 | 修改 Agent 数量、职责、模型 |
| 编辑 Prompt | 修改单个 Agent 的 Prompt |
| 查看内部讨论 | 展开 Agent 会议和线程 |
| 查看决策日志 | 查看关键决策原因 |
| 回滚检查点 | 回到历史状态 |
