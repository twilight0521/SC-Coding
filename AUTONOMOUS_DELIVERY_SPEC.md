# 自主项目交付流规范

## 1. 模块定位

自主项目交付流是 SuperCompany Coding 的主流程。

产品不应该让用户一个一个点击 Agent、逐个分配任务、逐个确认小步骤。正确体验是：

> 用户像 CEO 一样提出目标和边界；主控智能体像项目负责人一样组建团队、分配任务、调度执行、处理冲突、推动测试修复，并在关键节点向用户汇报。

用户不需要管理每个 Agent 的日常工作。用户只需要：

1. 给出场景和目标。
2. 确认或调整 Agent 团队方案。
3. 设置预算、权限和自主程度。
4. 启动项目。
5. 听主控智能体阶段汇报。
6. 只在关键风险点做决策。
7. 最后验收交付结果。

---

## 2. 核心体验原则

### 2.1 默认不是手动工作流

错误体验：

```text
用户创建 Agent
用户点击 PM Agent 开始
用户点击 Architect Agent 开始
用户点击 Frontend Agent 开始
用户点击 Backend Agent 开始
用户点击 Test Agent 开始
用户点击 Debug Agent 开始
```

正确体验：

```text
用户输入项目场景
  ↓
主控智能体生成执行方案
  ↓
用户一次性确认团队、模型、预算、权限
  ↓
主控智能体自动调度所有 Agent
  ↓
主控智能体定期汇报进度、风险、成本和下一步
  ↓
只有遇到高风险/超预算/范围变更时才请求用户确认
  ↓
系统自动完成代码、测试、修复、文档和交付
```

### 2.2 用户是 CEO，不是任务调度员

用户角色：

- 定方向
- 定边界
- 定预算
- 做关键决策
- 验收结果

用户不应该承担：

- 给每个 Agent 分配任务
- 手动衔接上下文
- 判断谁先谁后
- 手动转发错误日志
- 手动协调代码合并
- 手动判断每个小模块是否完成

这些都应该由 Orchestrator Agent 自动完成。

---

## 3. 标准自主交付流程

## 3.1 阶段 1：场景输入

用户输入自然语言，例如：

> 我要做一个 Windows 和 macOS 桌面 App，用来读取本地文件夹里的简历，然后根据岗位画像给出匹配度评分。

系统识别：

- 产品类型
- 运行平台
- 技术复杂度
- 数据敏感度
- 交付目标
- 约束条件
- 推荐技术栈
- 预算偏好
- 自主执行风险

输出：场景理解报告。

---

## 3.2 阶段 2：Agent 团队与执行方案生成

主控智能体自动生成：

1. 需要多少 Agent。
2. 每个 Agent 的职责。
3. 每个 Agent 推荐模型。
4. 每个 Agent 的备用模型。
5. 每个 Agent 的权限。
6. 每个 Agent 的系统 Prompt。
7. 阶段计划。
8. 任务依赖关系。
9. 成本预估。
10. 风险预估。
11. 需要用户提前确认的事项。

输出：`Execution Plan`。

---

## 3.3 阶段 3：用户一次性调整

用户可以在启动前调整：

- Agent 数量
- Agent 职责
- Agent 使用模型
- Agent Prompt
- Agent 权限
- 预算上限
- 技术栈
- 自主程度
- 汇报频率

这一步是“启动前配置”，不是执行中的频繁点击。

用户确认后，系统生成不可变的本轮执行快照：

```text
Execution Snapshot = 场景 + Agent Team + 模型路由 + Prompt + 权限 + 预算 + 汇报策略
```

---

## 3.4 阶段 4：主控智能体自动执行

启动后，Orchestrator Agent 负责：

1. 拆分任务。
2. 按依赖排序。
3. 并行调度可并行任务。
4. 把任务发送给对应 Agent。
5. 控制上下文。
6. 收集 Agent 产出。
7. 判断产出是否合格。
8. 触发 Code Review。
9. 触发 Integration。
10. 触发 Test。
11. 触发 Debug。
12. 记录所有关键决策。
13. 向用户汇报。

用户不需要逐个点击。

---

## 3.5 阶段 5：自动测试与修复闭环

默认闭环：

```text
代码生成
  ↓
代码合并
  ↓
运行 lint / typecheck / test / build
  ↓
如果失败，Debug Agent 读取错误日志
  ↓
修复代码
  ↓
再次测试
  ↓
最多自动修复 N 轮
  ↓
成功后进入下一阶段
```

默认参数：

| 参数 | 默认值 |
|---|---|
| 最大自动修复轮次 | 3 |
| 同一错误最大重试 | 2 |
| 自动升级模型次数 | 1 |
| 测试失败是否立即打断用户 | 否，先自动修复 |
| 连续失败后是否请求用户确认 | 是 |

---

## 3.6 阶段 6：阶段汇报

主控智能体在以下节点自动汇报：

1. 执行方案生成后。
2. 架构设计完成后。
3. 核心代码完成后。
4. 测试首次运行后。
5. 自动修复结束后。
6. 发现高风险问题时。
7. 接近预算上限时。
8. 最终交付前。
9. 最终交付后。

汇报不应该是每个 Agent 的碎片日志，而是项目负责人式摘要。

---

## 4. 自主程度设置

用户可以选择三种模式。

### 4.1 Supervised Mode：监督模式

适合高风险项目。

特点：

- 改核心架构前需要确认。
- 新增依赖前需要确认。
- 修改关键配置前需要确认。
- 每个阶段完成后暂停等待用户确认。

### 4.2 Balanced Mode：平衡模式，默认

适合大多数 MVP。

特点：

- 安全操作自动执行。
- 中低风险代码修改自动执行。
- 高风险命令、超预算、范围变更才打断用户。
- 每个阶段结束后汇报，但不一定暂停。

### 4.3 Autopilot Mode：自动驾驶模式

适合低风险 Demo 和个人项目。

特点：

- 除危险命令、敏感文件上传、删除文件、部署发布外，默认自动执行。
- 阶段汇报只做通知，不等待确认。
- 最终统一让用户验收。

---

## 5. 什么时候必须请求用户确认

以下情况必须打断用户：

| 场景 | 原因 |
|---|---|
| 删除文件或目录 | 可能造成不可逆损失 |
| 修改 `.env`、密钥、证书 | 涉及敏感信息 |
| 执行 `sudo`、`rm -rf`、`curl pipe sh` | 高风险命令 |
| 安装全局依赖 | 可能污染用户环境 |
| 向外部服务上传代码或数据 | 隐私风险 |
| 超过用户预算上限 | 成本风险 |
| 技术栈发生重大变化 | 范围变化 |
| 需求范围扩大超过预估 | 项目失控风险 |
| 发布、部署、推送远程仓库 | 对外影响 |
| 连续自动修复失败 | 需要用户选择方向 |

---

## 6. 哪些事情不应该打断用户

以下事情默认由主控智能体自动处理：

| 事项 | 处理方式 |
|---|---|
| 给 Agent 分配任务 | 自动 |
| 切换备用模型 | 在用户预设策略内自动 |
| 生成代码 | 自动 |
| 修改普通业务文件 | 自动，保留 Diff |
| 运行测试 | 自动 |
| 读取错误日志 | 自动 |
| 低风险 Bug 修复 | 自动 |
| 生成 README | 自动 |
| 生成交付报告 | 自动 |
| 调整任务优先级 | 自动，记录原因 |
| 合并非冲突代码 | 自动 |

---

## 7. 主控智能体汇报格式

### 7.1 阶段汇报模板

```text
# 阶段汇报：{阶段名称}

## 当前结论
{一句话说明当前阶段是否顺利}

## 已完成
- {完成事项 1}
- {完成事项 2}

## 当前发现
- {风险 / 约束 / 重要技术判断}

## 成本与进度
- 已用模型：{模型列表}
- 已消耗：{Token / 费用估算}
- 当前进度：{百分比}

## 下一步
- {下一阶段计划}

## 是否需要你决策
{不需要 / 需要，并说明选项}
```

### 7.2 风险汇报模板

```text
# 需要你决策：{问题标题}

## 问题
{发生了什么}

## 影响
{如果不处理会怎样}

## 可选方案
A. {方案 A}：优点 / 缺点 / 成本
B. {方案 B}：优点 / 缺点 / 成本
C. {方案 C}：优点 / 缺点 / 成本

## 主控建议
建议选择 {A/B/C}，原因是 {原因}。
```

---

## 8. 主控智能体内部机制

### 8.1 Orchestrator Runtime

Orchestrator Runtime 负责运行完整项目循环：

```ts
export interface OrchestratorRuntime {
  createExecutionPlan(input: ScenarioInput): Promise<ExecutionPlan>;
  createExecutionSnapshot(plan: ExecutionPlan, userOverrides: UserOverrides): Promise<ExecutionSnapshot>;
  startRun(snapshotId: string): Promise<ProjectRunId>;
  pauseRun(projectRunId: string, reason: PauseReason): Promise<void>;
  resumeRun(projectRunId: string, approval: ApprovalDecision): Promise<void>;
  emitReport(projectRunId: string, report: OrchestratorReport): Promise<void>;
  requestApproval(projectRunId: string, request: ApprovalRequest): Promise<void>;
}
```

### 8.2 内部状态机

```text
DRAFT_PLAN
  ↓
WAITING_USER_CONFIRMATION
  ↓
RUNNING
  ↓
PHASE_COMPLETED
  ↓
RUNNING
  ↓
WAITING_APPROVAL，可选
  ↓
RUNNING
  ↓
TESTING
  ↓
DEBUGGING，可选
  ↓
FINAL_REVIEW
  ↓
DELIVERED
```

---

## 9. UI 形态

主界面不应该是“让用户点每个 Agent”的控制台，而应该是：

# Project Command Center

核心区域：

1. 主控智能体汇报流。
2. 当前项目阶段。
3. 总进度。
4. 当前正在工作的 Agent。
5. 关键决策记录。
6. 成本与预算。
7. 风险与阻塞。
8. 最终交付物。

用户可以展开查看细节，但不需要默认面对所有子 Agent 的碎片输出。

---

## 10. 交付物

最终交付必须包含：

1. 可运行代码。
2. README。
3. 启动命令。
4. 测试结果。
5. 已知问题。
6. 本轮修改文件清单。
7. 关键技术决策。
8. 模型与 Agent 使用记录。
9. 后续迭代建议。
10. 如果失败，必须说明失败阶段、失败原因和下一步修复建议。

---

## 11. 产品结论

SuperCompany Coding 的完整形态不是“多 Agent 手动编排器”，而是：

> 用户给目标，主控智能体组队、分工、执行、测试、修复、汇报、交付。

用户不需要逐个管理 Agent。  
用户只需要像管理一个项目负责人一样，听汇报、做关键决策、验收结果。

---

## 11. 自主交付中的 Agent 内部协作

自主交付不是黑盒执行。每个阶段都应有对应的 Agent 协作记录。

### 11.1 阶段与会议映射

| 项目阶段 | 必须创建的 Agent 协作记录 |
|---|---|
| 场景理解 | 需求澄清会议 |
| 方案生成 | 产品方案评审 |
| 架构设计 | 架构评审会议 |
| 模块开发 | 开发同步线程 |
| 代码合并 | 集成评审线程 |
| 测试失败 | Bug 复盘会议 |
| 风险出现 | 风险仲裁会议 |
| 项目暂停 | 暂停快照和影响评估 |
| 项目恢复 | 恢复计划会议 |
| 最终交付 | 交付复盘会议 |

### 11.2 主控 Agent 的透明化职责

Orchestrator Agent 不仅要调度任务，还要解释团队内部发生了什么：

1. 哪些 Agent 参与了讨论。
2. 讨论了哪些问题。
3. 存在哪些分歧。
4. 最终采用了哪个方案。
5. 为什么没有采用其他方案。
6. 是否需要用户确认。

### 11.3 用户暂停后的恢复

用户暂停并微调后，Orchestrator Agent 必须组织一次“恢复计划会议”，确认：

1. 哪些任务已经过期。
2. 哪些任务需要重跑。
3. 哪些 Agent 需要重新读取上下文。
4. 哪些 Prompt 版本已变更。
5. 哪些模型切换会影响成本和质量。
6. 是否有必要创建新的 Checkpoint。
