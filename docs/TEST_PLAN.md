# 测试计划

## 1. Provider 测试

| 测试项 | 验收标准 |
|---|---|
| 新增 Provider | 可以保存名称、Base URL、模型名 |
| API Key 存储 | 不明文落盘 |
| 连接测试成功 | 显示成功、延迟、模型名 |
| 连接测试失败 | 显示明确错误 |
| OpenAI-Compatible | 可以调用兼容接口 |
| Minimax | 可以调用并返回内容 |
| DeepSeek | 可以调用并返回内容 |
| Claude | 可以调用并返回内容 |
| Gemini | 可以调用并返回内容 |
| 能力评分 | 可以手动编辑并保存 |

## 2. Agent 测试

| 测试项 | 验收标准 |
|---|---|
| 创建 Agent | 可以保存角色和 Prompt |
| 绑定主模型 | 执行时使用对应模型 |
| 绑定备用模型 | 主模型失败后切换 |
| 权限控制 | 无权限时不能写文件或执行命令 |
| 预算限制 | 超出预算时暂停 |

## 3. Router 测试

| 测试项 | 验收标准 |
|---|---|
| 架构任务推荐 | 推荐强推理模型 |
| 文档任务推荐 | 推荐低成本模型 |
| 长上下文任务推荐 | 推荐长上下文模型 |
| 多模态任务推荐 | 推荐多模态模型 |
| 用户手动覆盖 | 以用户选择为准 |
| 失败降级 | 可切换备用模型 |
| 决策日志 | 记录选择理由 |

## 4. 工作流测试

| 测试项 | 验收标准 |
|---|---|
| 输入需求 | Orchestrator 能拆任务 |
| 任务确认 | 用户可编辑任务 |
| 代码生成 | Coder 可生成文件修改 |
| Diff 展示 | 修改可见且可接受 / 拒绝 |
| 测试执行 | 能执行 npm test / build |
| Debug 修复 | 错误可进入修复循环 |
| 文档生成 | 能生成 README |

## 5. 安全测试

| 测试项 | 验收标准 |
|---|---|
| .env 过滤 | 默认不发送模型 |
| API Key 日志 | 日志中不出现明文 Key |
| 高风险命令 | 默认拦截 |
| 删除文件 | 必须确认 |
| 导出配置 | 默认不包含 API Key |

## 6. 跨平台测试

| 平台 | 测试项 |
|---|---|
| macOS | 安装、启动、Keychain、文件权限、终端 |
| Windows | 安装、启动、Credential Manager、路径处理、终端 |

## 7. MVP 验收场景

### 场景 1：只配置 Minimax + DeepSeek

1. 添加 Minimax M2.7。
2. 添加 DeepSeek Pro。
3. 添加 DeepSeek Flash。
4. 创建默认 Team。
5. 输入“做一个 Todo App”。
6. 系统拆任务。
7. 生成代码。
8. 运行测试。
9. 生成 README。

验收：项目可以运行。

### 场景 2：增加 Claude / Gemini

1. 添加 Claude。
2. 添加 Gemini。
3. 将 Architect 绑定 Claude。
4. 将 Long Context Reader 绑定 Gemini。
5. 对已有项目做重构。

验收：Router 能正确选择长上下文 / 架构模型。

### 场景 3：省钱模式

1. 设置省钱模式。
2. 输入简单文档任务。
3. 系统优先选择低成本模型。
4. 失败后升级 Pro。

验收：成本策略生效。
