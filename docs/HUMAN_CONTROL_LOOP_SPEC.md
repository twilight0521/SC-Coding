# HUMAN_CONTROL_LOOP_SPEC.md

# SuperCompany Coding 人类控制回路规范

## 1. 模块定位

本模块定义用户如何在不手动推进每个 Agent 的前提下，对项目保持最终控制权。

核心体验：

> AI 自动推进项目，用户随时可以暂停、观察、微调、恢复。  
> 用户不是执行员，而是拥有否决权和调整权的项目 CEO。

---

## 2. 控制原则

1. **默认自动执行**：用户确认项目方案后，主控 Agent 自动推进任务。
2. **关键节点确认**：涉及风险、预算、范围、隐私、发布时必须确认。
3. **随时可暂停**：用户可在任何阶段暂停项目。
4. **暂停后可微调**：可修改 Agent、模型、Prompt、任务、预算、权限。
5. **恢复前先评估**：主控 Agent 必须说明调整影响，再继续执行。
6. **保留完整记录**：所有暂停、修改、恢复、决策都进入审计日志。

---

## 3. 项目运行状态

```ts
type ProjectRunStatus =
  | 'draft'               // 草稿，尚未启动
  | 'planning'            // 规划中
  | 'waiting_user_confirm'// 等待用户确认
  | 'running'             // 自动执行中
  | 'paused_by_user'      // 用户主动暂停
  | 'paused_by_risk'      // 风险触发暂停
  | 'paused_by_budget'    // 预算触发暂停
  | 'adjusting'           // 用户调整中
  | 'replanning'          // 调整后重新规划中
  | 'resuming'            // 恢复执行中
  | 'completed'           // 完成
  | 'failed'              // 失败
  | 'cancelled';          // 用户取消
```

---

## 4. 用户可执行操作

### 4.1 暂停项目

按钮：`暂停项目`

触发后：

1. 停止派发新任务。
2. 等待正在执行的安全任务完成或中断。
3. 冻结文件写入。
4. 保存运行快照。
5. 生成暂停报告。

暂停报告包括：

- 当前阶段
- 已完成任务
- 正在执行任务
- 待执行任务
- 当前风险
- 当前成本
- 当前文件变更
- Agent 协作摘要

---

### 4.2 微调 Agent 团队

暂停后用户可以：

1. 新增 Agent。
2. 删除 Agent。
3. 禁用 Agent。
4. 修改 Agent 职责。
5. 修改 Agent 权限。
6. 修改 Agent 使用模型。
7. 修改 Agent Prompt。
8. 设置备用模型。
9. 设置成本上限。

每次修改必须生成影响评估。

---

### 4.3 微调 Prompt

Prompt 不只是系统内置文本，用户必须可以编辑。

Prompt 编辑范围：

1. Agent System Prompt。
2. Agent 工作规范。
3. Agent 禁止事项。
4. Agent 输出格式。
5. Agent 与其他 Agent 的沟通方式。
6. Agent 代码风格偏好。
7. Agent 风险敏感度。

Prompt 版本规则：

1. 每次修改生成新版本。
2. 保留历史版本。
3. 可回滚。
4. 继续执行时只影响后续任务。
5. 如用户选择，也可以重跑受影响任务。

---

### 4.4 微调任务范围

用户可以修改：

1. 删除某个功能。
2. 增加某个功能。
3. 降低复杂度。
4. 提高质量要求。
5. 修改验收标准。
6. 修改技术栈。
7. 修改交付形式。

修改后必须重新生成：

- 任务树
- Agent 分配
- 模型推荐
- 成本预估
- 风险评估
- 预计完成路径

---

### 4.5 恢复执行

按钮：`继续执行`

恢复前主控 Agent 必须汇报：

```text
我已根据你的调整重新规划项目。

变化：
1. 新增 UI Reviewer Agent，负责检查页面体验。
2. Frontend Agent 模型从 DeepSeek Flash 改为 Claude / Gemini 类型模型。
3. 删除原来的插件市场任务。
4. 需要重跑 2 个前端任务，后端任务不受影响。

预计影响：
- 成本增加约 15%
- 前端质量提升
- 项目范围更聚焦

是否继续？
```

用户确认后才恢复。

---

## 5. 干预模式

### 5.1 轻干预

用户只输入一句话：

```text
前端做得更像 Linear 一点，不要太花。
```

系统行为：

1. Orchestrator 判断影响范围。
2. 更新 UX / Frontend Agent Prompt。
3. 重新评估前端任务。
4. 必要时重跑前端相关任务。
5. 后端和数据库任务不受影响。

---

### 5.2 结构性干预

用户明确修改组织结构：

```text
增加一个 Security Agent，所有涉及本地文件和 API Key 的代码都让它审一遍。
```

系统行为：

1. 新增 Security Agent。
2. 生成默认 Prompt。
3. 推荐模型。
4. 将安全相关任务重新分配。
5. 对已完成的相关任务补充安全审查。

---

### 5.3 强制干预

用户强制要求停止某方向：

```text
不要做云同步，所有东西都必须本地化。
```

系统行为：

1. 立即暂停相关任务。
2. 从任务树中移除云同步相关任务。
3. 检查已生成代码是否包含云端逻辑。
4. 生成删除或回滚计划。
5. 请求用户确认后执行。

---

## 6. 风险拦截规则

主控 Agent 必须主动暂停的情况：

1. 任一 Agent 试图执行高风险命令。
2. 任一 Agent 建议大幅变更技术架构。
3. 任一 Agent 要删除大量文件。
4. 任一 Agent 要访问敏感文件。
5. 任一 Agent 要上传项目内容到外部服务。
6. 成本预测超过预算。
7. 测试连续失败超过设定轮次。
8. Agent 之间出现严重冲突。

---

## 7. UI 要求

项目运行页必须有：

1. `暂停项目` 按钮。
2. `继续执行` 按钮。
3. `调整团队` 按钮。
4. `修改 Prompt` 按钮。
5. `查看内部讨论` 按钮。
6. `查看决策日志` 按钮。
7. `查看影响评估` 面板。
8. `回滚到上一个检查点` 按钮。

---

## 8. 检查点机制

系统必须在关键阶段自动创建 Checkpoint。

默认检查点：

1. 用户确认项目方案后。
2. 架构设计完成后。
3. 数据库设计完成后。
4. 每个大模块完成后。
5. 测试通过后。
6. 用户手动暂停时。
7. 用户修改 Agent / Prompt / 模型前。
8. 用户恢复执行前。

Checkpoint 包括：

- 文件快照
- 数据库状态
- 任务状态
- Agent 配置
- Prompt 版本
- 决策日志
- 成本统计
- Agent 通信摘要

---

## 9. 验收标准

1. 用户可在运行中暂停项目。
2. 暂停后状态可完整恢复。
3. 用户可修改 Agent 数量、职责、模型、Prompt。
4. 系统可生成调整影响评估。
5. 用户可查看 Agent 内部协作。
6. 用户可回滚到最近检查点。
7. 高风险操作会自动暂停并请求确认。
8. 用户不需要逐个点击 Agent 才能推进项目。

